+++
title = "Family"
date = Date(2023, 03, 19) # 2021-10-01
icon = "r-project"
tags = ["family", "glm", ]
+++


# Family

拟合一个广义线性模型的通用函数是 `glm(fml, family, data, ...)`，在它的参数中，通过 `family` 设置模型的连接函数（link function）和响应变量的分布（distribution）。
